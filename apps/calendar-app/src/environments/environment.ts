export const environment = {
  // Google Maps JavaScript API key — restricted via HTTP referrer/API
  // restrictions in Google Cloud Console, not by secrecy (Maps JS keys
  // are always shipped in the client bundle by design).
  googleMapsApiKey: 'AIzaSyCxGekuqrzbSjJQjIFlK0LcP6AdgtP0DyE',
};
